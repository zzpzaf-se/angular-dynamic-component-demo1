import { AdDynDirective } from './ad-dyn.directive';

describe('AdDynDirective', () => {
  it('should create an instance', () => {
    const directive = new AdDynDirective();
    expect(directive).toBeTruthy();
  });
});
